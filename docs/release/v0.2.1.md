## Change

- **Shim 位置統一改 `~/.vibe-pipeline/bin/vbpl[.cmd]`**(原 Windows `%LOCALAPPDATA%\vibe-pipeline\vbpl.cmd` / POSIX `~/.local/bin/vbpl` 不一致)。理由:對齊 pyenv / cargo / nvm 慣例 + 跟既有 `~/.vibe-pipeline/bin/vbpl.exe` 同位置 + uninstall 一鍵 `rm -rf ~/.vibe-pipeline/` 全清不留孤兒
- install.ps1 / install.sh 自動 cleanup pre-v0.2.1 舊 shim 路徑
- uninstall.ps1 / uninstall.sh 重寫對齊新 layout(versions/ + current/ + bin/)+ 全 ASCII(避 Windows PS 5.1 ANSI 雷)+ legacy path auto-cleanup
- README / install.md path 描述同步
