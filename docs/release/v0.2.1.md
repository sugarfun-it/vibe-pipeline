## Highlights

- **推播修復** — v0.2.0 enduser 從未收到通知,根因找到並修。**必須升到 v0.2.1 才會收到推播。**

## Fix

`server/lib/push/tokenStore.ts` `gatewayUrl()` 沒套 v0.2.0 加的 `DEFAULT_GATEWAY_URL` fallback,導致 `registerToken` 全部 silent skip,gateway 上沒有任何 device token。修法:對齊 `gatewayToken.ts` / `fcm/index.ts` 走同一個 fallback。

## 升級

```bash
vbpl update
# 或 PWA Settings → 「套用更新」
```
