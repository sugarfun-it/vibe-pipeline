## Highlights

- **一行裝就好** — Windows `irm ... | iex` / Mac+Linux `curl ... | sh`,不必先跑 vite preview
- **`vbpl update` 一鍵更新** — CLI 或 PWA Settings → 「套用更新」都可以
- **tarball 變小,裝得快** — 預裝 stripped node_modules,enduser 裝完從 30-60 秒縮到 < 2 秒(823KB)

## 升級

```bash
vbpl update
# 或 PWA Settings → 「套用更新」
```
