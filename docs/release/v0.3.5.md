## Highlights
- **執行 / 審核 AI 掛掉時,直接告訴你為什麼** — 以前只顯「executor 失敗 code=1」一句看不懂,現在認證過期會明寫「認證失敗 (401) — 請重新登入後按繼續」,其他 API 錯誤也帶上原因。

## Fix
AI 連線失敗(登入過期、provider 報錯)時,畫面上的失敗原因之前是一句通用訊息,真正的錯誤被埋在 log 裡。現在會把實際原因抓出來顯在 ticket 上,一眼知道是該重新登入還是別的問題。

## 升級
```bash
vbpl update
# 或 PWA Settings → 「套用更新」
```
