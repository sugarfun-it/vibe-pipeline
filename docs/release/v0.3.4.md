## Highlights
- **更新後再也不會黑屏卡住** — 之前更新完偶爾會整片黑、要自己去瀏覽器「清除網站資料」才救得回來,現在 App 偵測到載不起來會自己清快取重載,不用你動手。

## Fix
更新後瀏覽器有時會抱著舊版本的殼不放,結果指到已經不存在的檔案 → 整片黑屏,而且重新整理也沒用(舊殼還在攔)。現在開啟 10 秒還沒載出來,就自動清掉舊快取再重載一次;真的還不行才會給一顆「清除快取並重試」鈕。

## 升級
```bash
vbpl update
# 或 PWA Settings → 「套用更新」
```
