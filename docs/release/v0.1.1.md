## Fix

- install.ps1 PowerShell 5.1 parse failure(line 171 backtick),Windows 預設 powershell.exe `irm | iex` 一行裝對絕大多數 user 直接掛 — 修純字串
- install.ps1 console 強制 UTF-8 encoding,Big5 console 不再吃中文亂碼
- push gateway `gatewayUrl()` 加 `DEFAULT_GATEWAY_URL` fallback,enduser 沒設 `PUSH_GATEWAY_URL` env 也能 auto-issue token

## Add

- `vbpl --version` / `-v` 印當前版本(原本只 fallback 印 help)

## Refactor

- `server/routes/projects.ts` 1103 → 894 行:抽 `withProject` / `withPipeline` / `withJsonBody` decorator 砍 37 處重複 prologue
- `SettingsPopover.tsx` 951 → 180 行:拆 ProjectTab / AITab / NotificationsTab / SecurityTab 各成獨立檔
- `FocusColumn.tsx` 1463 → 122 行:抽 `useFocusPipeline` hook + 拆 13 個子組件(FocusHeader / FocusActions / FocusTicketList / RunButton / TicketCard / IterStages / OverflowMenu 等)

## Release artifact

- v0.1.0 沒附正式 `.tar.gz` asset(install / update flow 走 `zipball_url` fallback),v0.1.1 起 `scripts/build-tarball.ts` 嚴格白名單組 tarball(`dist/` + `server/` + `cli/` + `shared/` + 配置檔)附 release
