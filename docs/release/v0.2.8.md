## Highlights
- **修好「執行」鈕** — 合併過的 pipeline 再加新 ticket,現在「執行」鈕會出現、按得下去(之前被藏住按不了)。
- **設定面板更省空間** — 四個分頁跟右上關閉的 ✕ 排成同一排。
- **手機設定面板** — 上面那塊多餘的空白修掉了。
- **確認視窗更好讀** — 刪除 / 重置 pipeline 的提示文字排版整理過。

## Fix
- 合併過的 pipeline 加新 ticket 後,「執行」鈕被誤藏 → 修正,加完就能直接跑。
- 手機開設定、四分頁改同排後,上方殘留的空白清掉。
- pipeline 刪除按下去立刻從列表消失,不再卡「沒反應 → 突然成功」。
- 用 vbpl 建 ticket 時,goal 寫太長會直接提醒你改短,不會白跑一輪。

## 升級
```bash
vbpl update
# 或 PWA Settings → 「套用更新」
```
