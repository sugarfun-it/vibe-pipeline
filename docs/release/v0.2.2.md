## Highlights

- **repo 搬到新家 `sugarfun-it`** — install / update 的 URL 都換了(舊 URL 還會 auto-redirect,不必手動改)
- **Windows 從 PWA 按更新真的會動了** — 之前按下去 backend 會被連坐殺掉,改用 Task Scheduler 把更新 task 丟出 backend 的 job object,跟 Chrome / Edge 同套路

## 升級

```bash
vbpl update
# 或 PWA Settings → 「套用更新」
```
