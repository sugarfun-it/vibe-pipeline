## Highlights
- **新 model 出來,不用更新 app 就能選** — Claude / Codex 一出新模型,設定裡的 model 下拉就會自己多出來。

## Feature
以前每次 Claude 或 Codex 出新模型,你得等我發新版、再跑一次更新才選得到。現在 model 跟 effort 的清單改從雲端拿,新模型出現後下拉選單會自動帶出來,你什麼都不用做。拿不到雲端清單時(離線之類)會自動用內建的舊清單,不會空掉或出錯。

## 升級
```bash
vbpl update
# 或 PWA Settings → 「套用更新」
```
