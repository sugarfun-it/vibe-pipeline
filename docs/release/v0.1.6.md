## Fix

- **update-helper.ps1 spawn 路徑炸 silent**:`server/lib/updater.ts:362` template literal 內 `"server\index.ts"` — `\i` 不是 JS valid escape sequence,TS 把 backslash 吃掉變 `"serverindex.ts"` → 生 PowerShell 是 `Join-Path $App "serverindex.ts"` → bun run 找不到檔 silent 死,新 backend 永遠起不來。改成 forward slash `"server/index.ts"`(PowerShell `Join-Path` 兩種 separator 都吃)
