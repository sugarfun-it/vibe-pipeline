## Highlights
- **修了 pipeline 偶爾卡在「執行中」停不掉** — 停止 / 繼續現在會確實生效,不會再卡住或莫名多跑一條。

## Fix
有時候跑 ticket 的 AI 卡住、或被系統誤判已經結束之後,它其實還在背景活著。這會讓 pipeline 一直顯示「執行中」、按停止沒反應、甚至重開又多跑出一個在搶寫同一份進度。這版會記住它的真實狀態,停止 / 重啟 / 當機回復時都能把它確實收乾淨,worktree 進度照樣保留,按繼續可以接著跑。

## 升級
```bash
vbpl update
# 或 PWA Settings → 「套用更新」
```
