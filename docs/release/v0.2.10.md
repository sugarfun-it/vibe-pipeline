## Highlights
- **推播註冊修好** — 啟用通知時 server 其實成功了卻被前端記成失敗,現在正確收到通知。
- **`vbpl pipeline status` 不再永遠顯示閒置** — 之前 CLI 查不到「跑中 / 排隊」(永遠 false),現在正確反映。
- **同步衝突交 AI 解修好** — claude 模式下 sync 撞衝突會卡住,現在能正常跑完。

## Fix
這版做了一輪全 repo 體檢:修掉上面幾個會影響使用的問題,外加一批看不見的內部清理讓後續更穩。沒有破壞性變更,直接升即可。

## 升級
```bash
vbpl update
# 或 PWA Settings → 「套用更新」
```
