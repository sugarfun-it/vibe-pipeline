node_modules/
dist/
.vite/
.DS_Store

# playwright
playwright-report/
playwright-report-real/
test-results/
playwright/.cache/
.playwright-browsers/
.tmp/

.vibe-pipeline

# perf bench output(benchmark 跑出來的 results,當時量測用,不該 track)
.perf-bench/

# tsc emit (build via Vite, no separate emit needed)
*.tsbuildinfo
src/**/*.js
src/**/*.js.map
server/**/*.js
server/**/*.js.map
shared/**/*.js
shared/**/*.js.map
tests/**/*.js
tests/**/*.js.map
!tests/**/*.mjs

# logs
*.log
npm-debug.log*

# env
.env
.env.*
!.env.example
firebase-service-account*.json

# FCM scratch / Firebase config (service account JSON、VAPID 抄寫等)
.fcm/

# editors
.vscode/
.idea/
*.swp
*.swo

# /loop runtime lock
.claude/scheduled_tasks.lock

# release tarball artifacts (build-tarball.ts)
release-tmp/
vibe-pipeline-v*.tar.gz
release-tmp/
update-flow.*
